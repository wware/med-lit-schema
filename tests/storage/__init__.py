"""
Tests for storage layer.

This package contains tests for storage interfaces, backends, and models.

Test Structure:

- **backends/**: Tests for backend implementations (SQLite, PostgreSQL)
- **models/**: Tests for SQLModel schema definitions

Run all storage tests:
    pytest tests/storage/ -v

Run backend tests only:
    pytest tests/storage/backends/ -v

Run model tests only:
    pytest tests/storage/models/ -v
"""
